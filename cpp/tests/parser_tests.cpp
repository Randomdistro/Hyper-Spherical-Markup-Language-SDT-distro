#include <catch2/catch_test_macros.hpp>
#include <string>

// Parser tests
TEST_CASE("HSML parser basic functionality", "[parser]") {
    // Parser test stub
    REQUIRE(true);
}

TEST_CASE("HSML lexer tokenization", "[parser]") {
    // Lexer test stub
    REQUIRE(true);
}
